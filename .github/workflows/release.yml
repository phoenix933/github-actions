name: Release

on: [push]

env:
  TEST_ENV: ${{ secrets.TEST_ENV }}
  NOT_SECRET: some test

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Print test
        run: |
        echo "${TEST_ENV}"
        echo "${NOT_SECRET}"
